"""Seed fake data into the unified database.

This script is a placeholder for Phase 1. Once all tables are created,
it will generate fake families, children, devices, tasks, sessions, etc.
"""


async def main() -> None:
    """Entry point for seeding fake data."""
    print("seed_fake_data.py: placeholder - implement after all tables are migrated")


if __name__ == "__main__":
    import asyncio

    asyncio.run(main())
